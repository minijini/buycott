enum AuthStatus { signout, progress, signin  }

enum LoginPlatform {
  facebook,
  google,
  kakao,
  naver,
  apple,
  none, // logout
}

enum CheckBoxStatus{
  all,
  age,
  terms,
  policy,
  event
}